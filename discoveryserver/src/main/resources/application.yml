server:
  port: 8001
eureka:
  server:
    enableSelfPreservation: false
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false

spring:
  cloud:
    config:
      discovery:
        enable: true